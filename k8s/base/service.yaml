apiVersion: v1
kind: Service
metadata:
  name: my-first-blog
  namespace: my-first-blog
spec:
  selector:
    app: my-first-blog
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
  type: LoadBalancer
